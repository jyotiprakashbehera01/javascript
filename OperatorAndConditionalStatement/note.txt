COMMENT:

//->It is single line COMMENT.
/**/->It has multi line COMMENT.
->it has mostly use describe our project.

OPERATORS:

->Used to some operation on data.
EX-
   a=b]->is calledas expresion.
   a,b]->operand.
   +   ->is called operator.

   1.ARITHOMATIC OPERATORS:
     -> +,-,/,*
     .Modulus ->%
     .Exponention ->**
      
   .UNARY OPERATORS
     -> It has content only single operand.
     .Incriment ->++
     .Decriment ->--
     a++(post incriment)
     ++a(pre incriment)


   2.ASSIGNMENT OPERATOR:
    = , += , -= , %= , **=
    (it has assign value.)
    EX-
       a+=1
       ->a=a+1

   3.COMPARISON OPERATOR:

   ->Equal to = (compair to check value)
   ->NotEqual to !=(compair to check value and data type)
   ->Equal to type ===
   ->NotEqual to Type !==
   -> >
   -> >=
   -> <
   -> <=

  4.LOGICAL OPERATOR:

  ->it can also give true or false(1,0).
    .logical AND &&
    .logical OR ||
    .logical NOT !


CONDITIONAL STATEMENT:

->To impliment some condition on the code.
ex-
 1. (if STATEMENT):
ex-
  let color;
  if(mode ==="dark-mode"){
    color = "black"
  }
 
2. (if else statement):
ex-
 let color;
 if(mode == "dark"){
    color = "black";
 } else {
    color = "white";
 }

syntax -> It has a term(rules).

3.(else-if statement):

->it has check multiple stae ment.
->



5.TERNARY OPERATOR:

->Condition? true outout : False Output
ex-
   age > 18 ? "adult" : "not adult" ;



