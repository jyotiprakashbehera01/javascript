LOOPs:

-> lOOPs are used to execute a piece of code again & again.

for lOOPs:

syantax:

it content 3 state ment.
Ex- for(let i = 0,1,2...n (intialization); i <= 7(stoping condition) ; i++(updatetion) ){
    console.log("jyoti");
}

INFANITE LOOP : A lOOPs never ends.
->stoping condition all ways tru.


WHILE lOOPs:
-->it has first intialization.
-->condition/stoping.
-->updatetion inside the lOOPs.
 
 Syntax:

while(condition){
    //do somework.
}

DO.WHILE LOOPs:

--> it has conditi check in last.

syantax:

do{

}while(condition);


FOR-OF-LOOPs:(speal loop)

-->it general help to iterate some special data type.
-->it use string and array.
-->it haas automaticaly updatetion,intialization,and condition proformance.

syantax:

for(let val of strVar){

}


FOR IN LOOPs:

-->it hass use object, array.
-->


* STRING :

-->A string is a sequence of characters enclosed in single quotes ('), double quotes ("), or backticks (`).
-->string is a sequence of charecter used to reprasent text.

-->CREATE STRIN:
1.let str = "jyotiprakash";
2.let str2 = 'jyoti';

-->String length:
1.str.length
Ex-  str
    'jyotiprakash'
     str.length
     12

-->STRING INDICES/index:
atr[0]
->it has behave like object.
ex - obj[key];


TEMPLATE LITERALS IN JS:
-->type of special string.
-->A way to have embedded exprations in strings
`this is a templete literal`

STRING INTERPOLATION :

-->to create string by dowing substitution of placeholder.
-->`string text ${expression}string text`.

ESCAPE charecter:

-->



STRING METHOD iN JS :
-->these are built-in function to maninpulate a string.
-->METHOD like a block of code .

1.str.toUpperCase()

