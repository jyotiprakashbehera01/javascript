FUNCTON :

-->it has block of code , that perform specific task .
   can be involked where ever need .
-->that create one time .
-->function helps to redundancy .
(input->block->output)


Work of function :

1.function deffination :

syntax :- 

function functionName(){
    //work 
}

2.function call :

syntax :-

functionName();

-->function can return a value .
-->function return at a one time after they cannot return .

function parameter:
syntsx:
function functionName(parameter1,parameter2){
    //work
}
-->it act like local variable scope.
-->th has block of scope.
-->function parameter like local variable of function.

ARROW FUNCTION:
--->It has compact way of writing function called as arrow function.
-->when it add a variable like(const , arr).

syntax:
const functionName = (par1,par2)=>{
    //work .
}

-->inside the function has 3 parameter :
1.value
2.index
3.array it self.

HIGHER ORDER FUNCTION AND METHOD:

-->It has take either parameter and  return them .


MAP :

-->Crete a new array with result of some operation ,
   The value it's callBack return are used form new array.

syntax:

arr.map(callBack function(val , ind , arr));
 
 let newArr = arr.map((val) =>{
    return val * 2;
 })


 FILTER :

 -->Crete a new array of element's ,that give the condition / filter .


 REDUCE :

 -->perform some operation & reduced the array to single value .
 -->it return that single value .
 