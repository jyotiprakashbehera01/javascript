ARRAY :
-->An array in JavaScript is a special object used to store multiple values in a single variable.
 Each value in an array is called an element, and each element has a numeric index starting from 0.
-->It has mutable.
-->It has don't content key value .
-->Simply it has collection of an iteam .
-->It has linear structure .

ARRAY INDICES:
-->arr[0],arr[1],arr[3]...it has index of an array.
when it help to replace valu.

LOOPING OVER AN ARRAY:
-->It has itrable(string , object , array).

1.for loop:
-->it use length property.
-->syntax:
for(let i=0;i<n;i++){
    //work 
}

2.for of loop:
-->it hass 

ARRAY METHOD :

1.push():
-->Add to end .
-->arr.push();
2.pop():
-->Delet and return in last array .
-->arr.pop();
3.toString():
-->Convert an array string.
-->arr.toString();
4.concat():
-->to joint multiple array & retyrn result .
-->arr1.concat(arr2);
5.unshift():
-->add to start.
-->arr.unshift();
6.shift():
-->delet for start and return them.
-->arr.shift();
7.slice():
-->return pice of the array.
-->slice(starting index , ending index);
8.splice():
-->change original array(add,delet,replace);
-->arr..splice();
